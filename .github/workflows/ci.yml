name: Reusable Workflow

on: push

jobs:
    Reusable-workflows:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout 
              run : echo "checkouting..."

            - name: Build
              uses: ./build.yaml

            - name: Test
              uses: ./test.yaml

    