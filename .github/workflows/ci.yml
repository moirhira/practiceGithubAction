name: CI

on: push

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      target: application

  test:
    needs: build
    uses: ./.github/workflows/test.yml
    with:
      build-result: Built by CI
    

