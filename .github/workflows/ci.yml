name: Reusable Workflow

on: push

jobs:
    Reusable-workflows:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout 
              run : echo "checkouting..."

            - name: Build
              run : echo "building..."

            - name: Test
              run : echo "Testing..."

    